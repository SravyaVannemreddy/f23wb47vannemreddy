extends layout

block content
  h1= title

    -var items = results
  div(class="container-fluid")
  div(class="row")
    each f, index in items 
     div(class="col-sm-2" onclick =`buy(${index})`) #{f.name},#{f.depth},#{f.origin}